# Transition SFX

Optional. Drop loud transition sounds (booms, whooshes, vine booms) here for the
`fancam` format. If empty, ClipFactory synthesizes a boom with ffmpeg so the
format still works with zero assets.

```
assets/sfx/transitions/vine_boom.mp3
```
